export const HOME_PATH = "/home";

export const HOME_BREADCRUMB_ITEMS = [{ title: "หน้าหลัก" }];

/** Right-hand link on the card header: "ทั้งหมด: N รายการ". */
export const totalLinkText = (total: number) => `ทั้งหมด: ${total} รายการ`;
